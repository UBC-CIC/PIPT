[pytest]
testpaths = tests
markers =
    property: Property-based tests using Hypothesis
    integration: Integration tests using mock embeddings
    slow: Tests that take longer to run

# Use dev profile by default (100 examples per property)
addopts = --hypothesis-seed=0
